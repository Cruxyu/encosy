[tool.poetry]
name = "pyecs"
version = "0.1.0"
description = ""
authors = ["baranynah <a.kovalenko.ai@yandex.ru>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
jupyterlab = "^3.5.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
